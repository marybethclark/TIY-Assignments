var word = "swimming";
var ingLocation =  word.indexOf("ing")

function verbing (x){
  if ingLocation === true
  // if length.x ==> 3 return x + 'ing';
  //else if length.x ==> 3 return x + 'ing';
  //else if length.x < 3 do nothing;


     }
     //verbing('swim')
       word.indexOf("ing");
