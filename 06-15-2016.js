function fixStart (){

var original = ('babble');
var firstCharacter = original.val(0);
// need to identify what that first letter IS
var lengthOfWord = length.original;
var firstLetterIs = original.(0);
// var subThe Asterisks = endPart + choppedFirstLetterOff;

// the output we want will look like
}
// fixStart('babble')
console.log(firstLetterIs);
// fixStart('babble'):'ba**le'
